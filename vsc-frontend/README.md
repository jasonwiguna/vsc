## VStream Connect Frontend (Website)

## Usage

```
# Build app
npm run build

# Rum app in docker
docker-compose up -d frontend

# Rebuild docker container (run app again after building)
docker-compose build frontend
```
